import { TPost } from '@/types/app'

export const getPosts = async (
	page: number,
	username: string
): Promise<TPost[]> => {
	return []
}

export const addPost = async (username: string): Promise<void> => {
	return
}
